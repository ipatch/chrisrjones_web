<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>chrisrjones.com<%= " | #{@page_title} " if @page_title %></title>
		<%= stylesheet_link_tag('application', :media => 'all') %>
		<%= javascript_include_tag('application') %>

	</head>
	<body>
	<!-- Part 1: Wrap all page content here -->

    <div id="wrap">
    	<div id="nav_100_width">
    		<!-- put navbar outside of container to get 100% width -->
    	<%= render 'layouts/nav' %>
    </div>

		<div class="container">

  		<% if current_user %>
  			Logged in as <%= current_user.email %>.
  			<%= link_to "Log Out", logout_path %>
			<% else %>
  			<%= link_to "Sign Up", signup_path %> or
  			<%= link_to "Log In", login_path %>
		<% end %>

  		<%= yield %>
			  
		</div>  
		<!-- need to figure out a way to insert a footer, i.e. one that sticks
		to the bottom of the web page -->
			<div id="push"></div>
    	</div>
		<div id="footer">
      		<div class="container">
        	<p class="muted credit"><id="copyright">&copy; chrisrjones.com / Chris Jones</p>
      		</div>
    	</div>

	</div> <!-- wrap div -->
	<%= render "shared/footer"%>
	</body>


<!-- 
	
<%= render(:partial => 'layouts/navigation', :locals => {:subjects => @subjects}) %>
 -->
	
</html>